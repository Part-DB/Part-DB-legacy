-> Upgrade to 0.2.0
- Load createdevicetables-UPDATE-to-V0.2.0.sql in the Database (if use MYSQLDUMPER Remove all Strings with --)

-> Upgrade to 0.1.6
- Load createdevicetables.sql new in the Database. (if use MYSQLDUMPER Remove all Strings with --)

-> Upgrade to 0.1.5

- DEL ../tools/footprints
- Config move from lib.php to config.php
- FIX it <base> ->>> </base> on nav.php remove Comments if Menue not Work.
- Load createtables.sql new in the Database.
